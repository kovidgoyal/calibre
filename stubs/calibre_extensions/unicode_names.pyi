def codepoints_for_word(word: str) -> frozenset[int]:
    "Return a set of integer codepoints for where each codepoint's name contains ``word``,"
    pass

def name_for_codepoint(cp: int) -> str | None:
    "Returns the given codepoint's name"
    pass
