def register_avif_image_format() -> bool:
    "Return True iff the AVIF image format was successfully registered with Qt when this module was imported"
    pass

def is_avif(data: bytes) -> bool:
    "Return True iff data starts with the header of an AVIF image"
    pass
